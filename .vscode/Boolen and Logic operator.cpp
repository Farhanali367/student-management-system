#include <iostream>
using namespace std;

int main() {
    int a = 5, b = 10;

    // Boolean variables
    bool x = true;
    bool y = false;

    cout << "Boolean values: x = " << x << ", y = " << y << endl;

    // Logical operators
    cout << "\nUsing logical operators:" << endl;
    cout << "(a < b && a > 0) = " << (a < b && a > 0) << endl;  // tru
